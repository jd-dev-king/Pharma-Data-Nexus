# Tables and SQL Viewer

The **Tables & SQL** button opens an in-scene database browser containing:

- All 38 normalized PostgreSQL tables grouped by enterprise domain
- A description of each table's role
- Ten SQL analysis queries used by the project
- Search filtering for tables and queries
- Copy-to-clipboard controls for SQL

The viewer is implemented in `tables-queries-data.js` and rendered by `app.js`.
